## 2. 任务拆解
1个点 = 2小时

### 任务 A：总开关接口 + 互通群标识

**目标**：进入列表页时获取互通总开关状态 + 标记哪些群是互通群（后台变更互通配置不推送前端；用户下次进入列表或刷新会话时再拉取，见 §3.2）

### 任务 B：消息过滤公共判断

**目标**：在 msgFilter 层统一处理互通群内的卡片消息可见性规则

### 任务 C：消息组件译文处理

**目标**：聊天页消息组件正确展示译文、列表组件处理"发来一条消息"